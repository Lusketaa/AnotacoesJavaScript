// Funções dentro de objetos no JavaScript

let calculadora = {                 // Fazendo uma calculadora com objetos, sintaxe mais resumida
    soma: (a,b) => a + b,
    divisao: (a,b) => a / b,
    multiplicacao: (a,b) => a * b
}

calculadora.soma(1,1)