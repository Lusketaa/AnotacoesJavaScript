// Objetos no JavaScript

let pessoa = { // Estrutura de JSON
    nome: 'Lucca',
    idade: 20
}

pessoa.profissao = 'programador front'

// Dessa forma a propriedade profissao seria adicionado ao meu objeto pessoa

delete pessoa.profissao

// Deleta o parâmetro do objeto

// É possível passar strings, number, arrays, funções e até mesmo outros objetos para dentro de um objeto